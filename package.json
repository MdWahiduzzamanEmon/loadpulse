{
  "name": "loadpulse",
  "version": "0.1.0",
  "private": true,
  "description": "Visual API testing dashboard with real-time animations",
  "workspaces": ["web", "agent"],
  "scripts": {
    "dev": "concurrently \"npm run dev:agent\" \"npm run dev:web\"",
    "dev:agent": "npm -w agent run dev",
    "dev:web": "npm -w web run dev",
    "build": "npm -w agent run build && npm -w web run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.8.3"
  }
}
